[build]
  base = "apps/web"
  command = "cd ../.. && corepack enable && pnpm install && pnpm --filter @localbms/shared build && pnpm --filter @localbms/web build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
