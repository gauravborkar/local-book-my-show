services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: localbms
      POSTGRES_PASSWORD: localbms
      POSTGRES_DB: localbms
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
